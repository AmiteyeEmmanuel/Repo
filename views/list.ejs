<%-  include('header'); %>

    <body>
        <div id="hero-container">
            <div class="Section-1" id="heading">
                <h1>
                    <%= ListDay%>
                </h1>
                <h3>
                    <%= ListTime %>
                </h3>
                <h2>
                    <%= ListTitle %>
                </h2>
            </div>

            <div class="Section-2">
                <h1>
                    CREATE YOUR TASK
                    <!-- Plan your daily activities, create a todolist that will help you know your schedule for a day with your phone, You don't need a pen or a note book when you have a todolist that can help you keep track of your daily activities with ease. -->
                </h1>
            </div>


        </div>


        <div class="box">
            <h2 class="box-header"> PERSONAL TASK</h2>
            <% newItems.forEach(function(item) {%>
                <form action="/deleteItem" method="post">

                    <div class="item">
                        <input type="checkbox" name="deletedItems" value="<%=item._id%>" onChange="this.form.submit()">
                        <p>
                            <%= item.name %>
                        </p>
                    </div>
                    <input type="hidden" name="listName" value="<%= ListTitle %>"></input>
                </form>
                <% }) %>

                    <!-- For loop -->
                    <!-- <% for (let i = 0; i < newItems.length; i++) { %>
                <div class="item">
                    <input type="checkbox">
                    <p>
                        <%= newItems[i].name %>
                    </p>
                </div>
                <% } %> -->


                    <form class="item" action="/" method="Post">

                        <input type="text" name="newItem" placeholder="New Item" autocomplete="off">

                        <button type="submit" name="list" value="<%= ListTitle %> " class="add">+</button>
                    </form>
        </div>
        <%- include('footer'); %>
    </body>